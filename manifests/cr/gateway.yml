apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: ext-host-gwy
spec:
  servers:
  - port:
      number: 8000
      name: http
      protocol: HTTP
    hosts:
    - example.otushomework